import styled from "styled-components";

export const Title = styled.span`
  font-size: 32px;
  font-weight: 600;
  color: #444;

  @media (max-width: 1024px) {
    font-size: 24px;
  }
`;

export default {
  Title,
};
